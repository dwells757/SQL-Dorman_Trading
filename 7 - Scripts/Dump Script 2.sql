USE [GMIDATA]
GO

SET NOCOUNT ON

SELECT
COLUMN_NAME + ','
FROM INFORMATION_SCHEMA.COLUMNS
WHERE (TABLE_NAME = 'GNACMFF1_File_Current')
AND (COLUMN_NAME NOT IN(
'DateLoaded',
'Record_Number'))
ORDER BY ORDINAL_POSITION

SET NOCOUNT OFF